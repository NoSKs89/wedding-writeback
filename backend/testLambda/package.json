{
    "name": "mongo-test-lambda",
    "version": "1.0.0",
    "description": "Minimal test for Mongoose connection in Lambda",
    "main": "handler.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
      "mongoose": "^7.0.0" 
    }
  }